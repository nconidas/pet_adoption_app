import 'package:flutter/material.dart';

class AdoptablePetBrowser extends StatelessWidget {
  const AdoptablePetBrowser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('All the Animals'));
  }
}
